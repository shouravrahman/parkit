import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import '@parkit/ui/src/app/globals.css'
import { MenuItem } from '@parkit/util/types'
import { ApolloProvider } from '@parkit/network/src/config/apollo'
import { SessionProvider } from '@parkit/ui/src/components/molecules/SessionProvider'
import { ToastContainer } from '@parkit/ui/src/components/molecules/Toast'
import { Container } from '@parkit/ui/src/components/atoms/Container'
import { Header } from '@parkit/ui/src/components/organisms/Header'
import { IsAdmin } from '@parkit/ui/src/components/organisms/IsAdmin'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Parkit | Admin',
  description: 'Generated by create next app',
}

const MENUITEMS: MenuItem[] = [
  { label: 'Garages', href: '/' },
  { label: 'Admins', href: '/manageAdmins' },
  { label: 'Users', href: '/users' },
]

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} bg-dark min-h-screen text-white`}>
        <SessionProvider>
          <ApolloProvider>
            <Header type="admin" menuItems={MENUITEMS} />
            <Container>{children}</Container>
          </ApolloProvider>
        </SessionProvider>
        <ToastContainer />
      </body>
    </html>
  )
}
